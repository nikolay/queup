{
  "name": "queup",
  "version": "1.0.12",
  "private": true,
  "type": "module",
  "scripts": {
    "assets": "node scripts/generate-assets.mjs",
    "assets:check": "node scripts/generate-assets.mjs --check",
    "check": "npm run assets:check && npm run check:extension",
    "check:extension": "python3 -m json.tool extension/manifest.json >/dev/null && node --check scripts/generate-assets.mjs && node --check extension/background.js && node --check extension/content.js && node --check extension/connect.js && node --check extension/popup.js"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
